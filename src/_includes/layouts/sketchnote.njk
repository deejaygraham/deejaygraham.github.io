{% extends 'layouts/post.njk' %}

{% block content %}

{{ content | safe }}

<section class="sketchnote">
    <table class="table is-fullwidth">
        <thead>
            <tr>
        {% if sketchnote.source %}<th>Source</th>{% endif %} 
        {% if sketchnote.speaker %}<th>Speaker</th>{% endif %}
        {% if sketchnote.event %}<th>Event</th>{% endif %}
            </tr>
        </thead>
        <tbody>
            <tr>
     {% if sketchnote.source %}<td>{% if sketchnote.source.url %}{% externalLink sketchnote.source.url, sketchnote.source.name %}
        {% else %}{{ sketchnote.source.name }}{% endif %}</td>
    {% endif %}
    {% if sketchnote.speaker %}<td>{% if sketchnote.speaker.url %}{% externalLink sketchnote.speaker.url, sketchnote.speaker.name %}
        {% else %}{{ sketchnote.speaker.name }}{% endif %}</td>
    {% endif %}
    {% if sketchnote.event %}<td>{% if sketchnote.event.url %}{% externalLink sketchnote.event.url, sketchnote.event.name %}
    {% else %}{{ sketchnote.event.name }}{% endif %}</td>
    {% endif %}
            </tr>
        </tbody>
    </table>
    <figure class="image">
        <img src="{{ sketchnote.url }}" alt="{{ sketchnote.alt }}" />
    </figure>
</section>

{% include "partials/ccboilerplate.njk" %}

{% endblock %}
