{#
<div class="grid">
{% for post in collections.posts | limit(12) %}
  <div class="cell">
    <article class="box">
      <a href="{{ post.url }}">{{ post.data.title }}</a>
    </article>
  </div>
{% endfor %}
</div>
#}

{# njk file helps with bulma rendering #}
<div class="grid is-col-min-12">
{% for post in collections.posts | limit(12) %}
	<div class="cell">
    		<div class="card">
{% if post.data.thumbnail %}
  			<div class="card-image">
				<figure class="image">
  					<img src="{{ post.data.thumbnail }}" />
				</figure>
			</div>
{% endif %}   			
	  		<div class="card-content">
    				<p class="title">{{ post.data.title }}</p>
				<p><a href="{{ post.url }}">{{ post.data.title }}</a></p>
{# date, tags #}
    			</div>
  		</div>
	</div>  
{% endfor %}
</div>
