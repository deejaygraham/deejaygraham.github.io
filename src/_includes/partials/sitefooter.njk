<footer class="footer">
  <div class="content has-text-centered">
	<nav aria-label="social media">
        	<ul>
          {% for item in socialmedia %}
            <a href="{{ item.url }}" aria-label="{{ item.name }}" target="_blank" rel="noreferrer"><img src="{{ site.url }}/img/{{ item.icon }}" /></li>
          {% endfor %}
        </ul>
      </nav>

	<div class="content has-text-centered">
		<!-- hosting etc -->
	</div>
	<div class="content has-text-centered">
      		<p>&copy; <time datetime="{{ site.buildTime | dateISO }}">{{ site.buildTime | dateYear }}</time></p>
    	</div>
</footer>

{# Scripts #}
<script type="text/javascript" src="{{ site.url }}/js/bulma.js"></script>
