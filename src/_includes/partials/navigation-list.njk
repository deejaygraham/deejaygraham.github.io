{# depends on the nav file in _data #}
{%- for item in nav %}
<a class="navbar-item" aria-label="{{ item.name }}" title="{{ item.name }}" href="{{ site.url }}{{ item.url }}">{{ item.name }}</a>
{% endfor -%}
