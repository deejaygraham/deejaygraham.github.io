/**
 * Linter for basic page content
 * @param {*} content 
 * @param {*} inputPath 
 * @param {*} outputPath 
 */
module.exports = function(content, inputPath, outputPath) {
  // https://www.11ty.dev/docs/config/#linters
  if (!outputPath.endsWith(".html")) return;

  
};
