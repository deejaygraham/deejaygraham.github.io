name: Install Playwright
description: Install Chromium and system dependencies for Playwright CI runs

runs:
  using: composite
  steps:
    - name: Install Playwright Chromium
      run: npx playwright install chromium --with-deps
      shell: bash
