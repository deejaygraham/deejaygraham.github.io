{
  "scripts": {
    "build": "npx @11ty/eleventy",
    "start": "npx @11ty/eleventy --serve",
    "test": "npx cypress run --e2e --browser chrome"
  },
  "dependencies": {
    "bulma": "^1.0.0",
    "cypress": "^13.7.2"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  }
}
